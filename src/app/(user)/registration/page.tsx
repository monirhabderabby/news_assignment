import RegistrationForm from "./components/registration-form";

const Login = () => {
  return (
    <div className="h-[calc(100vh-140px)] contain flex justify-center items-center">
      <RegistrationForm />
    </div>
  );
};

export default Login;
